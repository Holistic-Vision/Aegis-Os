Serverless examples (not deployed on GitHub Pages):
- stripe-webhook: validate subscription and issue access token
- ai-proxy: hide API key server-side

Use Cloudflare Workers / Netlify Functions / Supabase Edge Functions.
